package consulting.vonrehberg.education_app

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity ist Voraussetzung, damit `local_auth` auf Android die
// System-Authentifizierung (Fingerabdruck / Gesicht / Geraete-PIN) anzeigen kann.
class MainActivity : FlutterFragmentActivity()
